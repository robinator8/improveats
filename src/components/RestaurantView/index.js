// TODO (not currently needed)
// Implement with tabs that corrospond
// to the tabs that appear when you click on
// a restaurant on the website.

import RestaurantView from './RestaurantView';

export default RestaurantView;
